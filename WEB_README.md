1. **環境変数設定**：`.env.example`を`.env`へコピー。`DEMO_MODE=true`、`DEMO_WRITE_DB=false`でDB接続もAI呼出しも行いません。`DEMO_REGISTRY_JSON`に既存のClaude形式の中間JSON、`DEMO_EVIDENCE_JSON`に根拠付きintegrated.json、`DEMO_REPORT_JSON`に解析済み重調JSON、`CONTRACT_TEMPLATE`に名前定義付きxlsmを指定。本番は`DEMO_MODE=false`と既存のSupabase環境変数を使用し、管理者が`web_workspace.sql`を一度適用します。`DEMO_WRITE_DB=true`は実DBの読書きを有効にするため、通常の画面確認ではfalseのままにしてください。
2. **起動方法**：`run_local.bat`をダブルクリック。既存のPython環境（openpyxl）を使います。コマンドでは`python web_app.py`。終了は起動したウィンドウでCtrl+C。デモの採用・保留は起動中のメモリだけに保存され、再起動で戻ります。`DEMO_APPROVED_FIELDS`には、原本確認済みの重調項目だけをカンマ区切りで指定します。
3. **ブラウザURL**：`http://127.0.0.1:8765`。このPCだけで操作できます。外部へ実データやひな形を公開しません。

公開デモは python build_public_demo.py で生成します。公開対象は public-demo-dist の5ファイルだけです。ダミーデータで動作し、AI・DB接続・Excel生成は行いません。差分の操作は再読込で戻ります。ローカル版は従来どおり起動できます。

ローカルの「謄本PDFを追加」は同一住戸の建物・土地PDFを最大12件、合計40MBまで受け付けます。SHA256が一致する解析済みJSONを優先します。DEMO_MODE=trueでは未解析PDFを解析しません。新規解析はDEMO_MODE=falseと既存のOPENAI_API_KEY設定が必要です。既存読取プログラムは親フォルダから読み込みます（必要時のみREGISTRY_READER_ROOTで変更）。

画面からのPDF取込結果はローカルに保存・再起動時に復元します。実DBモードでは登録前に照合結果を表示し、「この案件で登録」で初めてDBへ保存します。候補が複数ある場合は登録を停止します。既存DB案件の確認・差分採用は従来どおりです。戸建て・判定不能・資料間の対応未確定はExcel生成を停止します。起動済みの場合はrun_local.batで画面だけを開きます。

## 重調PDFの案件取込

案件の「物件・資料」で重調PDFを選び、資料種類を「重調」にして読み取ります。謄本・重調・管理規約をまとめて投入できます。各PDFの資料種類を確認してください。重調は選択中の案件へ紐付きます。新規取込は先に案件登録を済ませてください。確定済みunit_idを重調保存処理へ渡します。

同じSHA256の解析済み重調JSONを再利用します。既存検証キャッシュ、`IMPORTANT_REPORT_CACHE_DIR`、ローカル出力内のキャッシュが対象です。デモでは未知PDFを停止し、DB書込み無効時は読取候補だけをローカル保存します。候補は再起動で復元し、既存の承認値・Excel出力へ自動採用しません。

本番はDB登録済み案件に限り既存の重調保存RPCへ渡し、保存後に資料・差分を再取得します。原本はローカル出力内へ保存します。複数資料のうち途中で失敗した場合、先に保存した資料は保持されるため、再読込後に確認してください。AIの再解析は未解析資料だけで行います。

## 新規謄本案件の本登録

管理者は `supabase/migrations` の登録用2ファイルを順に適用します。新規作成→建物・土地PDFの読取→「登録前の確認・候補を照合」→物件情報を確認→「この案件で登録」の順です。既存案件がある場合は再開か別案件の新規作成を選びます。DB登録は一括処理で、失敗時は全件を戻します。同じPDFの再投入は資料版を再利用します。

登録後のExcelはDB確定値を使い、差分を勝手に採用しません。confidence未取得・低confidence・根拠不足の差分は従来どおり採用を停止し、原本確認が必要です。原本PDFはローカル保存で、Storage転送は行いません。

## 管理規約・まとめて取込

`supabase/migrations/20260914024955_management_rules.sql` を適用後、新規作成で謄本・重調・管理規約を選択します。まず謄本を統合し、明示的な案件登録後に追加資料を読み取り・保存します。保存失敗時はSTEP1の「追加資料の保存を再実行」を使います。登録済み案件は残り、保存済み資料の再解析・重複登録はしません。未保存資料がある間はExcel生成を停止します。

管理規約は案件のbuilding_idへ保存し、重調とは別の根拠として表示します。読取可能な14項目を対象とし、原文・条文・ページを保持します。既存ひな形には適切な名前定義がないため、今回は管理規約のExcel転記は0項目です。新旧差分は明示採用まで承認値を変更せず、重調との記載差は原本確認の対象です。

`MANAGEMENT_RULES_CACHE_DIR` で解析済み規約キャッシュを指定できます。デモでは未知PDFをAPI送信しません。APIが429で停止した場合、テキストPDFのみ条文候補を保存し、全項目を要確認にします。複数候補から現在値を推測しません。画像PDFは利用制限解消後の再読取が必要です。

## 購入時重要事項説明書から開始

`supabase/migrations/20260914034420_purchase_baseline.sql` 適用後、新規作成の「購入時重要事項説明書を追加」を使用します。資料種類は重調と区別してください。スキャンは「スキャン原本の確認・訂正」で値・PDFページ・原文・項目別基準日を照合し、確認した項目だけ保存します。物件種別も原本で確認します。その後「登録前の確認・候補を照合」→「この案件で登録」。識別情報が不足・曖昧な場合は停止します。

原文AI出力と訂正履歴は別に保持し、資料は `purchase_important_explanation` / `historical_purchase_document` / `provisional` として保存します。過去資料の初期値です。未確認スキャン値を自動転記しません。再投入はSHA256で資料版を再利用し、既存案件の採用値を上書きしません。

登録済み案件の「資料を追加」から最新謄本・重調・管理規約を追加できます。案件の採用値と資料分野ごとの優先資料を比較し、採用／保留を経て、採用した根拠IDだけを `cases.purchase_values` へ記録します。Excelはこの採用値から既存生成処理を呼びます。対応する名前定義がない費用・取引条件等は画面確認のみです。新規の取引条件field_code追加は今回対象外です。

`PURCHASE_CACHE_DIR` は購入時重説のキャッシュルートです。`extracted_raw.json` はAPI原出力、`reviewed_fields.json` は原本確認・訂正記録です。`.env`、原本・キャッシュ・出力は共有リポジトリへ含めません。DEMO_MODEでは未知PDFのAI解析を禁止します。DB登録には管理者が業務モードを設定してください。
